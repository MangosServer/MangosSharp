
namespace Mangos.Common.Enums.Misc
{
    public enum TimeConstant
    {
        MINUTE = 60,
        HOUR = MINUTE * 60,
        DAY = HOUR * 24,
        WEEK = DAY * 7,
        MONTH = DAY * 30,
        YEAR = MONTH * 12,
        IN_MILLISECONDS = 1000
    }
}